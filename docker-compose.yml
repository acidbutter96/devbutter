version: '3.8'

services:
  mailpit:
    image: axllent/mailpit:latest
    container_name: devbutter_mailpit
    restart: unless-stopped
    ports:
      - "1025:1025"   # SMTP
      - "8025:8025"   # Web UI
    environment:
      - "LOG_LEVEL=info"
    volumes:
      - ./mailpit-data:/data
